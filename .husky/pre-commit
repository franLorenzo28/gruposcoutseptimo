#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

if ! command -v node >/dev/null 2>&1; then
	export PATH="/c/nvm4w/nodejs:/c/Program Files/nodejs:$PATH"
fi

if ! command -v node >/dev/null 2>&1; then
	echo "⚠️ Node no encontrado para Husky en este shell; se omite lint-staged."
	echo "   Consejo: abre VS Code de nuevo o ejecuta 'nvm use 20.20.0'."
	exit 0
fi

node ./node_modules/lint-staged/bin/lint-staged.js
